<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Notes</title>
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Slab:wght@100..900&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
    <h1>Welcome!</h1>
        <% data.forEach((record) => { %>
            <div class="content-container">
                <h1><%= record.name %> - by <%= record.author %></h1>
                <img src="<%= bookCovers[record.id - 1] %>" alt="Book Cover" class="book-cover">
                <p class="content"><span class="date-rating">Date read: <%= record.date_read %>. How strongly I recommend it: <%= record.rating %>/10</span> <br><%= record.book_summary %></p>
            </div>
            <% }); %>
        </div>
    </body>
</html>